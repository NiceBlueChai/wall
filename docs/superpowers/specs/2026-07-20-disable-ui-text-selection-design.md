<!-- 说明 Wall 非输入界面的文字选择修复边界与验证方式。 -->

# 禁止非输入界面文字选择

## 目标

应用导航、按钮、卡片及普通文案不可被鼠标拖选；文本输入框仍可正常选中文字。

## 实现

在应用根容器统一设置 `user-select: none`，并为文本输入控件恢复 `user-select: text`。不逐个组件添加规则，
避免后续新增界面再次泄漏浏览器默认行为。

## 边界

不修改右键菜单、视频预览、Figma 设计或其他交互。

## 验证

先在现有样式契约测试中增加失败断言，再添加最小 CSS 规则使其通过，并运行完整前端测试与构建。
